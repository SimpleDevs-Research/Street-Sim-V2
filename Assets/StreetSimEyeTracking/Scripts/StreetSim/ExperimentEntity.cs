using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExperimentEntity : MonoBehaviour
{   
    /*
    [SerializeField] private ExperimentEntity m_parent = null;
    [SerializeField] private List<ExperimentEntity> m_children = new List<ExperimentEntity>();
    [SerializeField] private string m_entityName = null;
    [SerializeField] private bool initializeOnStart = false;

    // Here's the logic:
    // We attach this object to anything that can be conceptually detectable
    // Any children that have colliders and don't have an `experimententity" attached to them mean they're associated with this parent
    // If anybody wants to interact with a child of this, then that child will refer to the parent instead

    private void Start() {
        if (initializeOnStart) Initialize(null, m_children);
    }

    private bool Initialize(Transform parent, out List<ExperimentEntity> children) {
        m_parent = parent;
        children = GetChildren();
        return false;
    }

    private List<ExperimentEntity> GetChildren() {
        foreach(Transform child in this.tranform) {

        }
        return null;
    }
    */
}
